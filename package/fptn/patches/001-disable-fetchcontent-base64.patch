--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,3 +1,11 @@
+find_package(base64 QUIET)
+
+if(base64_FOUND)
+  message(STATUS "Using system base64")
+else()
+
 include(FetchContent)
 FetchContent_Declare(
   base64
@@ -8,3 +16,5 @@
 )
 FetchContent_MakeAvailable(base64)
+
+endif()
