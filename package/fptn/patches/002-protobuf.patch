--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -22,6 +22,12 @@
 if (NOT USE_BORINGSSL)
     find_package(OpenSSL REQUIRED)
 endif()
+
+# OpenWrt: use system Protobuf (protoc + runtime)
+find_package(Protobuf REQUIRED)
+include_directories(${Protobuf_INCLUDE_DIRS})
+link_libraries(${Protobuf_LIBRARIES})
