config fptn 'main'
	# IP address of your FPTN server (not a domain!)
	# Needed to avoid routing loop when forcing all traffic via tun0
	option server_ip '0.0.0.0'

	# Optional: keep LAN access local (recommended)
	option keep_lan '1'
